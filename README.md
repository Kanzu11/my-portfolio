
  # Create Pages with TypeScript

  This is a code bundle for Create Pages with TypeScript. The original project is available at https://www.figma.com/design/ckjtWeK64cMoLTBR5TqbWl/Create-Pages-with-TypeScript.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  